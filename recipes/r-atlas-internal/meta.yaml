{% set version = "1.9" %}

package:
  name: r-atlas-internal
  version: {{ version }}

source:
  url: https://github.com/ebi-gene-expression-group/internal-ExpressionAtlas/archive/{{ version }}.tar.gz
  sha256: 4f91b41714b10fec86f85e10cc80c891fc1270f37e07ea4db559121ca4cb9baa

build:
  number: 0
  skip: true # [win32]
  rpaths:
    - lib/R/lib/
    - lib/

requirements:

  build:
    - {{ compiler('c') }}        # [not win]
    - {{ compiler('cxx') }}      # [not win]
    - make
  host:
    - r-xml
    - r-yaml
    - r-plyr
    - r-base >=4.6.0
    - bioconductor-biobase
    - bioconductor-biocgenerics
    - bioconductor-genomicranges
    - bioconductor-iranges
    - bioconductor-limma
    - bioconductor-summarizedexperiment
  run:
    - r-xml
    - r-yaml
    - r-plyr
    - r-base >=4.6.0
    - bioconductor-biobase
    - bioconductor-biocgenerics
    - bioconductor-genomicranges
    - bioconductor-iranges
    - bioconductor-limma
    - bioconductor-summarizedexperiment

test:
  commands:
    - $R -e "library(ExpressionAtlasInternal)"

about:
  home: https://github.com/ebi-gene-expression-group/internal-ExpressionAtlas
  license: GPL-3
  summary: A package exporting functions and classes used in the representation and analysis of EMBL-EBI Expression Atlas data.
  license_family: GPL3
